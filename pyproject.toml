[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "trading-platform-pro"
version = "0.1.0b1"
requires-python = ">=3.13"

[project.optional-dependencies]
dev = ["pytest>=8","pyyaml>=6","ruff>=0.6","mypy>=1.10"]
